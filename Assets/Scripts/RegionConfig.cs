using UnityEngine;

[CreateAssetMenu(fileName = "RegionConfig", menuName = "Scriptable Objects/RegionConfig")]
public class RegionConfig : ScriptableObject
{
    // Define ore drop rates for different regions

    // Define starting obstacle chances for different regions

    // Define the amount of ore needed to collect before moving to the next region


}
